{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@rocketseat/eslint-config": "^1.2.0",
    "eslint": "^8.39.0",
    "turbo": "^1.9.3"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  }
}